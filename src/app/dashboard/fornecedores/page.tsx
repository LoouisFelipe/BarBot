'use client';

import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function FornecedoresPage() {
  return (
    <div className="flex flex-col gap-4">
      <Card>
        <CardHeader>
          <CardTitle>Gerenciamento de Fornecedores</CardTitle>
          <CardDescription>
            Funcionalidade de gerenciamento de fornecedores para ser implementada.
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p>A tabela de fornecedores e as ações de gerenciamento aparecerão aqui em breve.</p>
        </CardContent>
      </Card>
    </div>
  );
}
